export enum ReviewStatus {
  Unread = 'unread',
  Published = 'published',
  Archived = 'archived'
}

export enum BookingStatus {
  Checkin = 'checkin',
  Checkout = 'checkout',
  InProgress = 'inprogress'
}
export enum UserStatus {
  Inactive = 'inactive',
  Active = 'active'
}
